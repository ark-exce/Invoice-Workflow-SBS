# Invoice-Workflow-SBS
Invoice Workflow SBS monitors a Google Drive folder for new PDF invoices, downloads and extracts text, then uses Google Gemini AI to pull key invoice details. Data is logged to a Google Sheet, an AI-drafted notification email is generated and sent to the client, all automatically.
